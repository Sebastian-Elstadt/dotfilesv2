# Skemos — hyprpaper.
# Images are generated per resolution and set at runtime by
# ~/.config/hypr/scripts/wallpaper.sh (via hyprctl hyprpaper ...), so nothing
# is preloaded here. Keep IPC enabled so the script can talk to the daemon.

ipc = on
splash = false
